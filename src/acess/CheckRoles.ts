// import ACCESS from "@/acess/acess";
//
// const checkAccess = (loginUser: any,needAccess = ACCESS.value.roles) => {
//   //获取当前用户信息
//   const user = loginUser.roles ?? ACCESS.value.isLogin;
//   //如果用户权限为不需要登录，直接返回true
//   if (needAccess === ACCESS.value.isLogin) {
//     return true;
//   }
//   if (ACCESS.value.roles.includes(needAccess))
//
// }
